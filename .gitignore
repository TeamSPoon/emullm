__pycache__/
*.pyc
runtime/
# Writable, runtime-mutated data. Everything the server creates lives under the
# single emullm_runtime container (config, logs, metrics, state, records); the
# older data/ location is kept ignored for backwards compatibility.
emullm_runtime/
data/
.pytest_cache/
*.egg-info/
.venv/
emullm_worker_*/request.json
emullm_worker_*/reply.json

# IDE / editor
.idea/

# Stray virtualenv graveyard and node artifacts (this is a Python project)
.dead-venvs/
node_modules/
package.json
package-lock.json

# Worker runtime logs/handoff (workers live under subagents/)
*.log
**/emullm_worker_*/request.json
**/emullm_worker_*/reply.json
.student/
subagents/codex_ide_worker/
subagents/copilot_worker_1/
